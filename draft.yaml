models:
  Order:
    description: longText nullable
    shiping_address: string nullable
    token: string nullable
    status: string nullable
    created_by: id foreign:users nullable
    updated_by: id foregin:users nullable
    relationships:
      hasOne: User
      belongsToMany: Product

controllers:
  Order:
    resource

seeders: Order
